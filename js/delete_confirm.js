//require <jquery.packed.js>

var pressed = 0;
var button_title;

//Disable multiple submit presses
jQuery(document).ready(function(){

	//When deleting a record
	//jQuery("#actions-menu-delete").click(function(){
	//	return confirm("Are you sure that you want to delete the current record?");
	//});

	//When removing a related record
	//jQuery("img").click(function(){
	//	if(jQuery(this).attr("alt") == "Move row up"){ //If the image clicked is the "Delete row" button
	//		alert(jQuery(this).attr("alt"));	//Ask for confirmation
	//											//Override default behaviour
	//		
	//	}
	//});
	
	//Increment
//	jQuery("input[type='submit']").click(function(){
//		pressed++;
//		button_title = jQuery(this).attr("value")
//	});

	//Reset
//	jQuery(":input").change(function(){
//		pressed = 0;
//	});
	
});

