<?php

class tables_admin_users {

	/*
		function role__permissions(&$record){
                if (!isAdmin() ) return array('edit'=>0,'view'=>0);
                return null;
        }

		function service__permissions(&$record){
                if (!isAdmin() ) return array('edit'=>0,'view'=>0);
                return null;
        }

		function accounting__permissions(&$record){
                if (!isAdmin() ) return array('edit'=>0,'view'=>0);
                return null;
        }
	*/
		
}

?>